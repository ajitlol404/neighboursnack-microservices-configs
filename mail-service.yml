server:
  port: 8083
spring:
  application:
    name: mail-service
  datasource:
    url: jdbc:h2:mem:mail_service_db
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database: H2
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: false
  h2:
    console:
      enabled: true
      path: /h2-console
